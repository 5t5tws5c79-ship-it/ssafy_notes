"""
06. 최대공약수 (유클리드 호제법)

> 두 자연수 a, b의 최대공약수는 다음 성질을 만족한다.
>
>     gcd(a, b) = gcd(b, a % b)
>     gcd(a, 0) = a
>
> 이 성질을 이용해 최대공약수를 반환하는 함수를 재귀로 작성하시오.

예시)
    gcd(12, 18)   #=> 6
    gcd(48, 18)   #=> 6
    gcd(17, 5)    #=> 1
"""


def gcd(a, b):
    # 종료 조건: b가 0이면 a가 최대공약수입니다.

    # 재귀 호출: gcd(b, a % b)
    pass


if __name__ == '__main__':
    print(gcd(12, 18))   # 6
    print(gcd(48, 18))   # 6
    print(gcd(17, 5))    # 1
    print(gcd(100, 75))  # 25
