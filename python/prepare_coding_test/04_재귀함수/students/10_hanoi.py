"""
10. 하노이의 탑

> 원반 n개가 시작 기둥에 크기 순으로 쌓여 있다.
> 아래 규칙을 지키며 모든 원반을 목표 기둥으로 옮기는 과정을 출력하고,
> 총 이동 횟수를 반환하는 함수를 재귀로 작성하시오.
>
>   1. 한 번에 원반 하나만 옮길 수 있다.
>   2. 큰 원반을 작은 원반 위에 올릴 수 없다.
>
> 힌트) n개를 옮기는 문제는 아래 3단계로 쪼갤 수 있다.
>   (1) 위쪽 n-1개를 [보조 기둥]으로 옮긴다.
>   (2) 가장 큰 원반 1개를 [목표 기둥]으로 옮긴다.
>   (3) 보조 기둥의 n-1개를 [목표 기둥]으로 옮긴다.

예시)
    hanoi(2, 'A', 'C', 'B')
    #=> 1번 원반: A -> B
    #   2번 원반: A -> C
    #   1번 원반: B -> C
    #   반환값 3
"""


def hanoi(n, start, target, via):
    # 종료 조건: 옮길 원반이 없으면 이동 횟수는 0입니다.

    # (1) 위쪽 n-1개를 보조 기둥(via)으로 옮기기 - 재귀 호출
    #     이때 목표는 via, 경유지는 target 이 됩니다.

    # (2) 가장 큰 n번 원반을 목표 기둥으로 옮기고 출력하기
    #     print(f'{n}번 원반: {start} -> {target}')

    # (3) 보조 기둥의 n-1개를 목표 기둥으로 옮기기 - 재귀 호출

    # 총 이동 횟수 = (1)의 횟수 + 1 + (3)의 횟수
    pass


if __name__ == '__main__':
    print(hanoi(1, 'A', 'C', 'B'))  # 이동 1회
    print('---')
    print(hanoi(2, 'A', 'C', 'B'))  # 이동 3회
    print('---')
    print(hanoi(3, 'A', 'C', 'B'))  # 이동 7회
